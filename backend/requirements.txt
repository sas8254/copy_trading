Django~=6.0
djangorestframework~=3.15
django-rest-knox~=5.0
psycopg[binary]~=3.2
django-environ~=0.11
django-cors-headers~=4.4
whitenoise~=6.7
gunicorn~=23.0

# Async / realtime
channels~=4.1
channels-redis~=4.2
daphne~=4.1

# Background tasks
celery~=5.4
redis~=5.0

# Broker (Zerodha)
# kiteconnect hard-pins autobahn==19.11.2, which conflicts with daphne's
# autobahn>=22. We only use its REST client (kiteconnect.connect.KiteConnect),
# which needs requests/dateutil, NOT autobahn. So kiteconnect itself is
# installed with --no-deps in the Dockerfile; its REST deps are listed here.
requests~=2.32
python-dateutil~=2.9
six~=1.16
# Used later for the Kite Ticker websocket (avoids autobahn entirely).
websocket-client~=1.8
